/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "image.tmdb.org",
          "image.tmdb.orgnull"
        ],
      },
};

export default nextConfig;
